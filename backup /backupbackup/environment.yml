# Conda environment for Personal Portfolio Backend
# Usage: conda env create -f environment.yml
# Then:  conda activate portfolio-agent
name: portfolio-agent
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - -r requirements.txt
